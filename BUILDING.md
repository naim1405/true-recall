# Building and Distributing Wayback

## Quick Build

```powershell
cd wayback\build
cmake --build . --config Release
```

The standalone executable will be at: `build\Release\wayback.exe` (~500KB)

---

## For End Users

The Release build is **fully standalone** - no dependencies required!

### Distribution Package

```powershell
# Create distribution folder
mkdir wayback-dist
cd wayback-dist

# Copy files
copy ..\build\Release\wayback.exe .
copy ..\README.md .
copy ..\CHANGELOG.md .
copy ..\LICENSE .

# Create ZIP
Compress-Archive -Path * -DestinationPath wayback-v1.1.zip
```

**That's it!** Users can download and run `wayback.exe` immediately.

---

## Build from Source

### Requirements

- Windows 10 or later
- Visual Studio 2022 (or Build Tools)
- CMake 3.15+

### Steps

```powershell
# Clone or download the repository
cd wayback

# Create and configure build directory
mkdir build
cd build
cmake ..

# Build Release version (recommended)
cmake --build . --config Release

# Build Debug version (with console)
cmake --build . --config Debug
```

---

## Creating an Installer (Optional)

---

## Optional: Create an Installer

### Using Inno Setup

1. Download [Inno Setup](https://jrsoftware.org/isinfo.php)
2. Create installer script
3. Compile to create `wayback-setup.exe`

See Inno Setup documentation for details.

---

## Distribution Checklist

- [ ] Build in Release mode
- [ ] Test on clean Windows install
- [ ] Verify no missing DLL errors
- [ ] Test hotkey registration and functionality
- [ ] Test tray icon and menu
- [ ] Test config file creation and editing
- [ ] Check file size (~500KB)
- [ ] Include README.md and CHANGELOG.md

---

## Publishing

### GitHub Releases (Recommended)

1. Tag version: `git tag v1.1 && git push origin v1.1`
2. Create GitHub Release
3. Upload assets:
   - `wayback.exe`
   - `wayback-v1.1.zip` (with README, CHANGELOG, LICENSE)
   - Optional: `wayback-setup.exe` (installer)

### Release Assets

Recommended uploads:
- `wayback.exe` - Standalone executable
- `wayback-v1.1.zip` - Complete package
- `Source code` - Auto-generated by GitHub

---

## Notes

- The Release build requires **no runtime dependencies**
- Maximum compatibility across Windows 10/11 systems
- For most users, just share `wayback.exe`
- Unsigned binaries show SmartScreen warning (normal for new apps)
- Optional: Code sign with certificate to avoid warnings

---

## Version Updates

When releasing a new version:

1. Update version in `TrayIcon.cpp` (About dialog)
2. Update `CHANGELOG.md`
3. Update README.md (version number at bottom)
4. Commit changes
5. Tag release: `git tag v1.x`
6. Build and test
7. Create GitHub release
8. Upload binaries

Recommendation: **Start with ZIP**, add installer later if needed.

---

## Sharing the Binary

Safe to share via:
- GitHub Releases
- Personal website
- Direct download link
- Cloud storage (OneDrive, Dropbox)

**Security note:** Users will see SmartScreen warning for unsigned binaries. This is normal for new applications.

---

## Final Notes

The Release build is **production-ready** and can be shared immediately.

No additional runtime dependencies means **maximum compatibility** across Windows 10/11 systems.

For most users, just share `wayback.exe` + `README.md` in a ZIP file.
